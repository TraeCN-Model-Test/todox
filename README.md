# TodoX

一个使用Flutter和GetX框架开发的现代化待办事项管理应用。

## 功能特性

- ✅ 创建、编辑和删除待办事项
- 🏷️ 为待办事项设置优先级（高、中、低）
- 📅 设置截止日期并自动提醒
- 🔍 搜索和筛选待办事项
- 📊 查看任务完成统计
- 🌙 支持亮色/暗色主题切换
- 💾 本地数据持久化存储
- 📱 响应式设计，适配多种屏幕尺寸
- 🎨 统一的颜色管理系统
- 📋 多种排序方式（按时间、优先级、标题、截止日期）
- 📈 完成率统计展示
- 🔄 滑动操作（编辑、删除）
- 🏷️ 标签系统（支持多个标签）

## 技术栈

- **框架**: Flutter
- **状态管理**: GetX
- **数据存储**: Hive
- **架构模式**: MVVM (Model-View-ViewModel)
- **路由管理**: GetX Navigation
- **依赖注入**: GetX Dependency Injection
- **本地数据库**: Hive (轻量级NoSQL数据库)
- **UI组件**: Material Design 3
- **代码生成**: Hive Code Generator (用于类型安全的数据库操作)

## 项目结构

```
lib/
├── core/                  # 核心功能
│   ├── constants/        # 常量定义（颜色、尺寸等）
│   ├── themes/          # 主题配置
│   └── utils/           # 工具类
├── data/                # 数据层
│   ├── models/          # 数据模型（Todo等）
│   └── repositories/    # 数据仓库（Hive服务）
├── domain/              # 业务逻辑层
│   ├── entities/        # 实体类
│   └── usecases/        # 用例
├── presentation/        # 表示层
│   ├── bindings/        # 依赖注入
│   ├── controllers/     # 控制器（TodoController等）
│   ├── pages/           # 页面（主页、创建页等）
│   └── widgets/         # 组件（TodoItemWidget等）
└── routes/              # 路由配置
```

## 开始使用

### 前置要求

- Flutter SDK (版本 3.0 或更高)
- Dart SDK (版本 2.17 或更高)
- 适用于 Android/iOS/macOS/Windows/Linux 的开发环境

### 安装步骤

1. 克隆仓库到本地
   ```bash
   git clone https://github.com/your-username/todox.git
   cd todox
   ```

2. 安装依赖
   ```bash
   flutter pub get
   ```

3. 运行应用
   ```bash
   flutter run
   ```

## 开发指南

### 添加新功能

1. 在 `lib/data/models/` 中创建数据模型
2. 在 `lib/data/repositories/` 中实现数据访问逻辑
3. 在 `lib/presentation/controllers/` 中创建控制器
4. 在 `lib/presentation/pages/` 中创建页面
5. 在 `lib/presentation/bindings/` 中创建绑定
6. 在 `lib/routes/` 中配置路由

### 代码规范

- 遵循 Dart 官方代码风格指南
- 使用有意义的变量和函数名
- 为公共API添加注释
- 编写单元测试
- 所有颜色必须从 `core/constants/app_colors.dart` 获取，不在UI组件中直接使用 `Color` 或 `Colors`
- 使用GetX进行状态管理和依赖注入
- 遵循MVVM架构模式，保持业务逻辑与UI分离

## 构建应用

### Android

```bash
flutter build apk --release
```

### iOS

```bash
flutter build ios --release
```

### Web

```bash
flutter build web --release
```

## 贡献指南

欢迎提交问题报告和功能请求！如果您想贡献代码，请遵循以下步骤：

1. Fork 这个仓库
2. 创建您的功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交您的更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开一个 Pull Request

## 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 致谢

- [Flutter](https://flutter.dev/) - 跨平台UI工具包
- [GetX](https://pub.dev/packages/get) - 状态管理和路由管理
- [Hive](https://pub.dev/packages/hive) - 轻量级、快速的键值数据库
- [Slidable](https://pub.dev/packages/flutter_slidable) - 滑动操作组件

## 应用截图

### 主页
- 待办事项列表展示
- 搜索和筛选功能
- 统计数据展示
- 排序选项

### 创建/编辑页面
- 标题和描述输入
- 优先级选择
- 截止日期设置
- 标签管理

### 主题切换
- 亮色/暗色主题支持
- 统一的颜色管理系统
- 响应式设计适配
